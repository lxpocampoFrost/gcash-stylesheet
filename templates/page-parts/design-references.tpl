<div class="section-block --map-to-tokens -reference" 
    data-name="Colors" data-token-index="color">
    <div class="ds-description">
        <p>
            These are the color selections and tints used throughout the Design System stylesheets. It's advisable to use the named variables instead of the absolute hex value to maintain consistency on its application.
        </p>
        <p>
            Colors are with absolute hex values. Tints on the otherhand, are those with alpha mix and can be used on top of other colors.
        </p>
    </div>
</div>

<div class="section-block --map-to-tokens -reference" 
    data-name="Spacings" data-token-index="space">
    <div class="ds-description">
        <p>
            These are the spacing references used to spearate elements. These are specifically used for margins, and paddings.
        </p>
        <p class="short-description">
            The spacing uses multiples of 4s and 8s.
        </p>
    </div>
</div>

<div class="section-block --map-to-tokens -reference" 
    data-name="Icon Sizes" data-token-index="icon-size">
    <div class="ds-description">
        <p>
            These are the different size variants used for icons.
            <br /> The size depends on the context and application.
        </p>
    </div>
</div>

<div class="section-block --map-to-tokens -reference" 
    data-name="Font Scales" data-token-index="font-size">
    <div class="ds-description">
        <p>
            These are the range of type scales used for the site's typesetting. However, it is advisable to use this only for reference. For all application, It's advisable to use the pre-defined typesetting properties.
        </p>
    </div>
</div>